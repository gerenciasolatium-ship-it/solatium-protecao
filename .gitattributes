# Normaliza finais de linha para LF no repositório (evita CRLF em builds Docker/CI Linux).
* text=auto eol=lf

# Binários — nunca normalizar.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
*.woff binary
*.woff2 binary
